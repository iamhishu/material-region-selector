import { ExampleComponent, TestComponent } from '.'

describe('ExampleComponent', () => {
  it('is truthy', () => {
    expect(ExampleComponent).toBeTruthy()
  })
})
describe('TestComponent', () => {
  it('is truthy', () => {
    expect(TestComponent).toBeTruthy()
  })
})
